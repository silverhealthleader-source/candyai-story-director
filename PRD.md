# Python 응용: API 활용 국내 여행지 추천 프로그램 PRD

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 제품명 | 국내 여행지 추천 프로그램 (가칭: Korea Travel Planner) |
| 문서 버전 | v1.0 |
| 제품 형태 | Python 기반 CLI 프로그램 |
| 대상 환경 | Python 3.10 이상, Windows/macOS/Linux 터미널 |
| 기본 API 조합 | OpenAI API + Kakao Local API |
| 핵심 산출물 | 원본 JSON, Markdown 여행 리포트, README |

## 2. 제품 개요

사용자가 여행 날짜를 입력하면 LLM이 해당 시기에 적합한 국내 여행 지역을 추천하고, 장소 검색 API가 추천 지역의 맛집을 조회한 뒤, 두 결과를 결합해 1일 여행 계획이 포함된 Markdown 리포트를 생성하는 CLI 프로그램이다.

이 제품의 핵심은 개별 API 호출 자체보다 다음 데이터 파이프라인을 안정적으로 구현하는 데 있다.

`사용자 날짜 입력 → LLM의 구조화된 지역 추천 → 장소 API 맛집 검색 → LLM 최종 리포트 생성 → 파일 저장`

## 3. 배경 및 문제 정의

API 사용 경험이 적은 Python 학습자는 다음 지점에서 어려움을 겪는다.

- REST API의 요청과 응답 구조를 실제 프로그램 흐름에 연결하기 어렵다.
- LLM의 자유 형식 응답을 다음 API의 입력으로 활용하기 어렵다.
- 인증, 쿼터, 네트워크, JSON 파싱 오류에 대한 대응 방식을 설계하기 어렵다.
- API 키를 안전하게 관리하면서 실행 가능한 프로젝트를 구성하기 어렵다.

본 프로그램은 서로 다른 두 종류의 API를 하나의 흐름으로 연결하고 중간 데이터를 JSON으로 명확히 관리함으로써 위 문제를 해결한다.

## 4. 제품 목표

### 4.1 사용자 목표

- 날짜 하나만 입력해 국내 여행지 추천 결과를 받을 수 있다.
- 추천 지역의 날씨 특성, 행사 후보, 추천 이유, 맛집, 1일 일정을 한 문서에서 확인할 수 있다.
- 실행 결과를 JSON과 Markdown 파일로 보관하고 재확인할 수 있다.

### 4.2 학습 목표

- GET과 POST 요청의 차이 및 REST API의 요청·응답 구조를 설명할 수 있다.
- LLM 응답을 JSON으로 파싱하고 다음 API 호출의 입력으로 연결할 수 있다.
- 인증, 쿼터, 네트워크, 빈 검색 결과, 파싱 실패에 대응할 수 있다.
- API 키를 코드가 아닌 `.env` 또는 환경변수에서 관리할 수 있다.

### 4.3 성공 지표

- 유효한 날짜와 API 키가 제공되면 원본 JSON과 Markdown 리포트가 모두 생성된다.
- LLM 1차 응답은 필수 스키마로 파싱된다.
- 장소 검색 실패 또는 결과 0건이어도 최종 리포트 생성까지 진행된다.
- 실행 로그 및 저장 파일에 API 키가 노출되지 않는다.
- 잘못된 날짜 입력 시 외부 API를 호출하지 않고 사용법과 오류를 출력한다.

## 5. 대상 사용자

### 주요 사용자

- Python 기본 문법과 패키지 설치 경험이 있는 초급 개발자
- 외부 API 및 LLM API 연동을 처음 학습하는 사용자

### 사용 전제

- Python 3.10 이상이 설치되어 있다.
- OpenAI API 키와 Kakao REST API 키를 발급받을 수 있다.
- 터미널 명령어를 실행하고 `.env` 파일을 설정할 수 있다.

## 6. 범위

### 6.1 MVP 포함 범위

- `argparse` 기반 CLI
- `--date` 및 단축 옵션 `-date` 지원
- `YYYY-MM-DD` 형식과 실제 존재하는 날짜 검증
- OpenAI API를 이용한 단일 국내 지역 추천
- LLM 결과의 JSON 파싱 및 필수 필드 검증
- Kakao Local API를 이용한 추천 지역 맛집 최대 5곳 검색
- 추천 결과와 맛집 목록을 이용한 Markdown 리포트 생성
- 단계별 진행 로그 출력
- 오류 수집 및 결과 파일 기록
- `results/` 폴더 자동 생성
- 원본 JSON과 Markdown 파일 저장
- `.env` 기반 API 키 관리
- 설치·실행·보안 안내를 포함한 README 제공

### 6.2 MVP 제외 범위

- 웹 또는 모바일 UI
- 실제 예약, 결제, 길찾기, 교통편 검색
- 실시간 날씨 및 공식 행사 API 연동
- 숙박 시설 추천
- 여러 날짜에 걸친 상세 여행 일정
- 사용자 계정 및 추천 이력 동기화
- 추천 정확도 보장

### 6.3 후속 확장 범위

- 2~3개 지역 동시 추천 및 지역별 맛집 검색
- 같은 날짜 결과 캐싱
- 실제 기상청 날씨 API 및 공공데이터 행사 API 연동
- 예산, 동행 유형, 출발지, 여행 취향 등 추가 입력

## 7. 핵심 사용자 시나리오

### 정상 흐름

1. 사용자가 API 키를 환경변수 또는 `.env`에 설정한다.
2. `python travel_planner.py --date "2026-09-15"`를 실행한다.
3. 프로그램이 날짜와 API 키를 검증한다.
4. LLM이 날짜에 적합한 국내 도시 한 곳을 JSON으로 추천한다.
5. 프로그램이 JSON을 검증하고 `recommended_city`를 추출한다.
6. Kakao Local API에서 `도시명 맛집`을 검색한다.
7. LLM이 추천 정보와 맛집 목록을 바탕으로 Markdown 리포트를 작성한다.
8. 프로그램이 원본 JSON과 Markdown 리포트를 `results/`에 저장한다.
9. 터미널에 완료 메시지와 저장 경로를 출력한다.

### 장소 검색 장애 흐름

1. 1차 지역 추천까지 정상 완료된다.
2. 장소 API에서 인증, 쿼터, 네트워크 오류 또는 빈 결과가 발생한다.
3. 프로그램이 오류를 `errors` 목록에 추가하고 맛집 목록을 빈 배열로 설정한다.
4. 최종 리포트의 맛집 섹션을 `데이터 없음`으로 표시한다.
5. 나머지 결과 파일을 정상 저장한다.

### LLM JSON 파싱 실패 흐름

1. 첫 LLM 응답을 JSON으로 파싱하거나 스키마로 검증하지 못한다.
2. 프로그램이 필수 키만 포함한 JSON을 다시 출력하도록 1회 재요청한다.
3. 재요청이 성공하면 정상 흐름을 계속한다.
4. 재요청도 실패하면 오류를 안내하고 종료한다. 불완전한 추천 데이터로 장소 API를 호출하지 않는다.

## 8. 기능 요구사항

### FR-01. CLI 입력

- 프로그램은 `python travel_planner.py --date "YYYY-MM-DD"` 형식으로 실행할 수 있어야 한다.
- 과제 명세와의 호환을 위해 `-date`도 동일한 옵션으로 지원해야 한다.
- 날짜 옵션은 필수이다.
- 인자가 누락되거나 유효하지 않으면 `argparse` 사용법과 구체적인 오류 메시지를 출력하고 종료 코드 2로 종료해야 한다.
- 달력에 존재하지 않는 날짜도 거부해야 한다. 예: `2026-02-30`.

### FR-02. 환경 설정 및 API 키 검증

- 프로그램은 `.env`와 운영체제 환경변수에서 키를 읽어야 한다.
- 필수 환경변수는 다음과 같다.
  - `OPENAI_API_KEY`
  - `KAKAO_REST_API_KEY`
- 필수 키가 없으면 외부 API를 호출하지 않고 즉시 종료해야 한다.
- 오류 메시지에 필요한 변수명과 설정 방법을 안내하되 키 값은 출력하지 않아야 한다.
- `.env`는 Git 추적 대상에서 제외해야 하며 `.env.example`에는 변수명과 예시 placeholder만 포함해야 한다.

### FR-03. 1차 여행지 추천 생성

- 입력은 검증된 여행 날짜이다.
- LLM은 대한민국 내 도시 또는 지역 한 곳을 추천해야 한다.
- 응답은 설명 문구나 Markdown 코드 펜스가 없는 JSON 객체여야 한다.
- 추천 결과는 다음 필수 스키마를 만족해야 한다.

```json
{
  "recommended_city": "제주",
  "weather": "9월 중순의 일반적인 날씨 특성 요약",
  "events": ["행사 또는 축제 후보"],
  "reason": "추천 근거 2~4문장"
}
```

#### 필드 규칙

| 필드 | 타입 | 필수 | 규칙 |
|---|---|---:|---|
| `recommended_city` | string | 예 | 비어 있지 않은 국내 도시 또는 지역명 |
| `weather` | string | 예 | 해당 시기의 일반적 날씨 요약 |
| `events` | array[string] | 예 | 1~3개 권장, 확정 정보가 아닐 수 있음을 표현 |
| `reason` | string | 예 | 추천 근거 2~4문장 |

- 실시간 정보로 오인하지 않도록 날씨와 행사는 일반적·예상 정보이며 변경될 수 있음을 프롬프트와 리포트에 명시해야 한다.

### FR-04. LLM 응답 파싱 및 재시도

- 응답을 JSON으로 파싱하고 필수 키와 타입을 검증해야 한다.
- 첫 파싱 또는 검증 실패 시 오류 원인을 내부 오류 목록에 기록해야 한다.
- JSON만 다시 생성하도록 수정 프롬프트를 사용해 최대 1회 재요청해야 한다.
- 무한 재시도를 허용하지 않는다.
- 최종 실패 시 사용자에게 실패 단계를 알리고 비정상 종료해야 한다.

### FR-05. 맛집 검색

- 검색어는 기본적으로 `{recommended_city} 맛집`으로 구성한다.
- Kakao Local 키워드 검색 API를 GET 방식으로 호출한다.
- 최대 5개 결과를 사용한다.
- API 응답은 내부 공통 모델로 정규화해야 한다.

```json
{
  "name": "장소명",
  "address": "도로명 또는 지번 주소",
  "category": "음식점 > 한식",
  "url": "https://place.map.kakao.com/...",
  "x": 126.0,
  "y": 33.0
}
```

- 좌표가 제공되면 문자열 응답을 숫자로 변환한다.
- 주소는 도로명 주소를 우선 사용하고, 없으면 지번 주소를 사용한다.
- 선택 필드가 없을 경우 빈 문자열 또는 `null`을 허용하되 항목 자체 때문에 전체 처리를 중단하지 않는다.
- 검색 결과가 0건이면 빈 배열을 반환하고 `EMPTY_RESULT` 오류를 기록한 뒤 계속 진행한다.

### FR-06. 최종 리포트 생성

- 입력은 1차 추천 JSON, 맛집 목록, 오류 목록이다.
- LLM을 사용해 Markdown 형식의 한국어 리포트를 생성해야 한다.
- 리포트에는 다음 섹션이 순서대로 포함되어야 한다.
  1. 제목 및 여행 날짜
  2. 추천 지역
  3. 추천 이유
  4. 날씨 요약
  5. 행사/축제
  6. 맛집 추천
  7. 1일 일정 제안(오전/오후/저녁)
  8. 참고 및 오류 요약
- 맛집 목록이 비어 있으면 `데이터 없음`을 명시해야 한다.
- 장소명, 주소, URL, 좌표 등 API가 제공한 사실 데이터는 임의로 변경하거나 새로 만들어내지 않도록 프롬프트에서 제한해야 한다.
- 날씨와 행사 정보는 실제 운영 정보와 다를 수 있으므로 방문 전 공식 채널 확인 안내를 포함해야 한다.

### FR-07. 결과 데이터 저장

- 프로그램은 프로젝트 루트에 `results/` 폴더가 없으면 생성해야 한다.
- 파일 이름은 사용자가 입력한 여행 날짜를 기준으로 결정한다.
  - `results/YYYY-MM-DD_travel_data.json`
  - `results/YYYY-MM-DD_travel_plan.md`
- 원본 JSON은 UTF-8, 들여쓰기 형식으로 저장하며 한글을 유니코드 이스케이프하지 않아야 한다.
- 원본 JSON의 최소 구조는 다음과 같다.

```json
{
  "requested_date": "2026-09-15",
  "generated_at": "2026-08-19T12:34:56+09:00",
  "recommendation": {
    "recommended_city": "제주",
    "weather": "...",
    "events": ["..."],
    "reason": "..."
  },
  "restaurants": [],
  "errors": []
}
```

- 동일 날짜 파일이 이미 있으면 MVP에서는 기존 파일을 덮어쓴다.
- 저장 실패 시 오류 메시지와 종료 코드를 반환해야 한다.
- API 요청 또는 응답 헤더, API 키, 전체 인증 정보는 저장하지 않아야 한다.

### FR-08. 진행 로그

- 표준 출력에 다음 단계와 결과를 간결하게 표시해야 한다.

```text
[1/3] 1차 추천 생성 중(LLM)...
  - 추천 지역: 제주
[2/3] 맛집 검색 중(Kakao Local)...
  - 맛집 5곳 검색 완료
[3/3] 최종 리포트 생성 중(LLM)...
  - 리포트 생성 완료

완료! results/2026-09-15_travel_plan.md 를 확인하세요.
```

- 로그에는 API 키, 인증 헤더, 민감한 원문 요청 정보를 출력하지 않아야 한다.
- 장애가 복구 가능한 경우 계속 진행한다는 사실을 로그로 알려야 한다.

### FR-09. 오류 관리

- 오류는 내부적으로 다음 공통 구조로 수집해야 한다.

```json
{
  "step": "place_search",
  "type": "AUTH_ERROR",
  "message": "HTTP 401"
}
```

#### 오류 정책

| 상황 | 처리 | 최종 리포트 진행 |
|---|---|---:|
| API 키 미설정 | 설정 안내 후 즉시 종료 | 아니요 |
| 날짜 형식/값 오류 | 사용법 출력 후 즉시 종료 | 아니요 |
| LLM 추천 요청 실패 | 오류 안내 후 종료 | 아니요 |
| LLM JSON 파싱 실패 | 1회 재요청, 재실패 시 종료 | 조건부 |
| 장소 API 인증/쿼터/네트워크 오류 | 빈 맛집 목록과 오류 기록 | 예 |
| 장소 검색 결과 0건 | `EMPTY_RESULT` 기록 | 예 |
| 최종 리포트 LLM 호출 실패 | 저장된 데이터를 이용해 템플릿 기반 리포트 생성 | 예 |
| 파일 저장 실패 | 대상 경로 안내 후 종료 | 아니요 |

## 9. 비기능 요구사항

### NFR-01. 보안

- 비밀 키를 소스코드, README, 로그, 예제 결과 파일에 포함하지 않는다.
- `.gitignore`에 `.env`를 추가한다.
- HTTP 오류 객체나 요청 헤더 전체를 그대로 로그에 남기지 않는다.

### NFR-02. 안정성

- 모든 외부 API 요청에 연결 및 응답 타임아웃을 설정한다. 권장값은 10~30초이다.
- 외부 호출과 JSON 파싱은 `try-except`로 처리한다.
- JSON 파싱 재시도는 정확히 최대 1회로 제한한다.
- 부분 장애가 허용된 단계에서는 가능한 결과를 저장한다.

### NFR-03. 유지보수성

- CLI, 설정, LLM, 장소 검색, 파일 저장 책임을 함수 또는 모듈 단위로 분리한다.
- API 제공자별 응답 필드를 내부 공통 데이터 구조로 정규화한다.
- 프롬프트는 코드의 비즈니스 로직과 구분해 관리한다.
- 함수와 공개 모델에 타입 힌트를 적용한다.

### NFR-04. 사용성

- 오류 메시지는 실패 원인, 영향, 사용자가 취할 조치를 포함해야 한다.
- 모든 사용자 출력과 결과 문서는 한국어를 기본으로 한다.
- 완료 시 생성된 두 파일의 경로를 모두 안내한다.

### NFR-05. 호환성

- Python 3.10 이상에서 동작해야 한다.
- 운영체제별 경로 차이를 피하기 위해 `pathlib`을 사용한다.
- 결과 파일은 UTF-8로 읽고 쓸 수 있어야 한다.

## 10. 권장 기술 구성

| 영역 | 권장 도구 |
|---|---|
| CLI | Python `argparse` |
| 환경변수 | `python-dotenv`, `os.getenv` |
| LLM | OpenAI Python SDK |
| 장소 검색 | `requests` 또는 `httpx`, Kakao Local API |
| 데이터 모델/검증 | `dataclasses` 또는 Pydantic |
| 날짜 처리 | `datetime.date`, `datetime.fromisoformat` |
| 경로/저장 | `pathlib`, `json` |
| 테스트 | `pytest`, `unittest.mock` 또는 `respx` |

## 11. 권장 시스템 구조

```text
travel-planner/
├─ travel_planner.py          # CLI 진입점
├─ config.py                  # 환경변수 로딩 및 검증
├─ models.py                  # 추천/맛집/오류 데이터 모델
├─ llm_service.py             # 1차 추천 및 리포트 생성
├─ place_service.py           # Kakao Local 검색 및 정규화
├─ storage.py                 # JSON/Markdown 저장
├─ prompts.py                 # LLM 프롬프트
├─ requirements.txt
├─ .env.example
├─ .gitignore
├─ README.md
├─ results/
└─ tests/
   ├─ test_cli.py
   ├─ test_llm_service.py
   ├─ test_place_service.py
   └─ test_storage.py
```

초기 구현 규모가 작다면 모든 기능을 `travel_planner.py` 하나에 작성해도 과제 조건은 만족할 수 있다. 다만 테스트와 오류 처리를 명확히 하기 위해 위와 같은 모듈 분리를 권장한다.

## 12. API 및 데이터 흐름

### 12.1 LLM 1차 추천

- 방식: OpenAI API POST 요청
- 입력: 여행 날짜, 출력 스키마, 국내 지역 제한, 최신 사실 보장 불가 안내
- 출력: 구조화된 JSON 객체
- 후속 입력: `recommended_city`를 Kakao 검색어에 사용

### 12.2 Kakao Local 장소 검색

- 방식: HTTP GET
- 엔드포인트: Kakao Local 키워드 검색 API
- 인증: `Authorization: KakaoAK {REST_API_KEY}`
- 주요 파라미터: `query`, `size=5`
- 출력: `documents`를 내부 맛집 목록으로 정규화

### 12.3 LLM 최종 리포트

- 방식: OpenAI API POST 요청
- 입력: 추천 JSON, 정규화된 맛집 목록, 오류 요약
- 출력: Markdown 텍스트
- 실패 대안: 정해진 Markdown 템플릿에 원본 데이터를 삽입해 로컬 생성

## 13. 프롬프트 요구사항

### 13.1 추천 프롬프트

- 입력 날짜를 명확히 포함한다.
- 추천 범위를 대한민국 국내 지역 한 곳으로 제한한다.
- 정확한 JSON 스키마와 필드 타입을 제시한다.
- JSON 외 설명과 코드 펜스를 금지한다.
- 날씨는 해당 시기의 일반적 특성으로 작성하도록 한다.
- 행사는 후보로 표현하고 실제 개최 여부 확인이 필요함을 반영한다.

### 13.2 재시도 프롬프트

- 이전 응답의 의미는 가능한 한 유지한다.
- 누락되거나 잘못된 필드를 필수 스키마에 맞춘다.
- JSON 객체 외 텍스트를 출력하지 않도록 한다.

### 13.3 리포트 프롬프트

- 필수 섹션과 순서를 명시한다.
- 맛집 데이터가 없으면 `데이터 없음`으로 쓰도록 한다.
- 입력에 없는 장소명, 주소, URL을 만들어내지 않도록 한다.
- 1일 일정은 오전·오후·저녁 수준으로 작성한다.
- 사용자가 방문 전 날씨와 행사 운영 여부를 확인하도록 안내한다.

## 14. 테스트 및 인수 기준

### AC-01. 정상 실행

**Given** 유효한 날짜와 두 API 키가 설정되어 있고 API가 정상 응답할 때  
**When** 사용자가 프로그램을 실행하면  
**Then** `results/`에 원본 JSON과 Markdown 리포트가 생성되고 완료 경로가 출력된다.

### AC-02. 날짜 누락

**Given** 날짜 옵션이 없을 때  
**When** 프로그램을 실행하면  
**Then** 사용법을 출력하고 외부 API 호출 없이 종료 코드 2로 종료한다.

### AC-03. 잘못된 날짜

**Given** `2026-02-30` 또는 다른 형식의 값이 입력될 때  
**When** 프로그램을 실행하면  
**Then** 올바른 형식을 안내하고 외부 API를 호출하지 않는다.

### AC-04. API 키 누락

**Given** 필수 키 중 하나가 설정되지 않았을 때  
**When** 프로그램을 실행하면  
**Then** 누락된 환경변수명과 설정 방법을 출력하고 즉시 종료한다.

### AC-05. LLM JSON 1차 파싱 실패 후 성공

**Given** 첫 응답이 유효하지 않고 두 번째 응답이 유효할 때  
**When** 추천을 생성하면  
**Then** 재요청은 1회만 수행되고 이후 장소 검색과 리포트 생성을 계속한다.

### AC-06. LLM JSON 재시도 실패

**Given** 두 응답 모두 필수 스키마를 만족하지 않을 때  
**When** 추천을 생성하면  
**Then** 장소 API를 호출하지 않고 오류를 안내하며 종료한다.

### AC-07. 장소 검색 0건

**Given** 장소 API가 정상 응답하지만 결과가 없을 때  
**When** 전체 흐름을 실행하면  
**Then** JSON에 빈 `restaurants`와 `EMPTY_RESULT`가 기록되고 리포트에는 `데이터 없음`이 표시된다.

### AC-08. 장소 API 오류

**Given** Kakao API가 401, 403, 429 또는 네트워크 오류를 반환할 때  
**When** 전체 흐름을 실행하면  
**Then** 오류가 정규화되어 저장되고 최종 리포트 생성은 계속된다.

### AC-09. 최종 LLM 호출 실패

**Given** 1차 추천과 장소 검색이 완료됐지만 최종 LLM 호출이 실패할 때  
**When** 결과를 저장하면  
**Then** 로컬 템플릿 기반 Markdown 리포트가 생성되고 오류 요약에 실패가 남는다.

### AC-10. 보안

**Given** 프로그램 실행이 성공하거나 실패했을 때  
**When** 로그, README, JSON, Markdown을 검사하면  
**Then** 실제 API 키 또는 인증 헤더가 포함되어 있지 않다.

## 15. README 필수 내용

- 프로그램 소개와 전체 데이터 흐름
- Python 버전 및 사전 준비 사항
- 의존성 설치 방법
- OpenAI 및 Kakao API 키 발급·설정 방법
- `.env.example`을 `.env`로 복사해 사용하는 방법
- Windows PowerShell 및 macOS/Linux 환경변수 설정 예시
- CLI 실행 예시
- 생성되는 결과 파일과 확인 방법
- 대표 오류 및 해결 방법
- API 키를 저장소에 커밋하면 안 되는 이유
- 날씨와 행사 정보의 한계 및 방문 전 확인 안내

## 16. 개발 우선순위

### P0 — 필수

- CLI와 날짜 검증
- 환경변수 및 API 키 검증
- LLM 구조화 추천과 1회 재시도
- Kakao 맛집 검색과 실패 허용
- 최종 Markdown 리포트
- JSON/Markdown 저장
- 보안 처리와 README

### P1 — 품질

- 데이터 모델 검증
- API 타임아웃 및 오류 유형 정규화
- 최종 LLM 실패 시 템플릿 폴백
- 단위 테스트와 API 모킹

### P2 — 선택

- 복수 지역 추천
- 날짜별 캐싱
- 실제 날씨 및 행사 API 추가

## 17. 구현 단계 제안

1. 프로젝트 구조, 의존성, `.env.example`, `.gitignore` 구성
2. CLI 및 날짜·키 검증 구현
3. 추천 데이터 모델과 LLM JSON 생성·재시도 구현
4. Kakao Local 검색과 공통 맛집 모델 변환 구현
5. Markdown 리포트 생성 및 로컬 폴백 구현
6. 결과 저장과 진행 로그 구현
7. 실패 시나리오 단위 테스트 작성
8. README 작성 및 실제 통합 실행 검증

## 18. 완료 정의(Definition of Done)

- 모든 P0 요구사항이 구현되어 있다.
- 정상·잘못된 날짜·키 누락·LLM 파싱 실패·장소 API 실패·검색 0건 시나리오가 검증되어 있다.
- 정상 실행 시 요구된 두 결과 파일이 `results/`에 생성된다.
- 장소 API 장애가 있어도 Markdown 리포트가 생성된다.
- 저장소와 산출물 어디에도 실제 API 키가 포함되지 않는다.
- README만 읽고 새 사용자가 설치, 키 설정, 실행, 결과 확인을 수행할 수 있다.
- 예제 결과가 필수 JSON 스키마와 Markdown 섹션을 만족한다.

## 19. 주요 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| LLM이 JSON 외 문장을 출력 | 파싱 실패 | 구조화 출력 기능 사용, 스키마 검증, 1회 수정 요청 |
| 날씨·행사 정보가 부정확함 | 사용자 오인 | 일반 정보임을 명시하고 공식 채널 재확인 안내 |
| Kakao API 인증 또는 쿼터 오류 | 맛집 데이터 누락 | 오류 기록 후 빈 목록으로 리포트 계속 생성 |
| API 제공자 SDK 변경 | 실행 실패 | 버전 고정 및 README에 설치 버전 명시 |
| 키가 로그나 저장소에 노출 | 보안 및 과금 사고 | `.env`, `.gitignore`, 마스킹, 민감 정보 저장 금지 |
| 동일 날짜 실행 시 파일 덮어쓰기 | 이전 결과 손실 | MVP 동작을 README에 명시하고 후속 버전에서 타임스탬프/캐싱 제공 |

## 20. 의사결정 사항

- MVP는 학습 난이도와 반복 호출량을 낮추기 위해 추천 지역을 한 곳으로 제한한다.
- 기본 장소 제공자는 국내 장소 검색과 좌표·URL 제공이 가능한 Kakao Local로 정한다.
- 장소 검색 장애는 전체 프로그램 실패로 취급하지 않는다.
- 추천 데이터가 유효하지 않으면 이후 단계의 기준이 없으므로 전체 흐름을 중단한다.
- 최종 LLM 호출 실패 시 이미 확보한 데이터를 잃지 않도록 로컬 Markdown 템플릿을 사용한다.
- 같은 날짜 재실행 시 덮어쓰기를 기본으로 하고 캐싱은 후속 기능으로 분리한다.
